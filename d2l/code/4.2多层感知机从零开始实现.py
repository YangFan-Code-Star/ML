import torch
from torch import nn
from d2l import torch as d2l

batch_size = 256
train_iter, test_iter = d2l.load_data_fashion_mnist(batch_size)

num_inputs, num_outputs, num_hiddens = 784, 10, 256

W1 = nn.Parameter(torch.randn(
    num_inputs, num_hiddens, requires_grad=True) * 0.01)
b1 = nn.Parameter(torch.zeros(num_hiddens, requires_grad=True))
W2 = nn.Parameter(torch.randn(
    num_hiddens, num_outputs, requires_grad=True) * 0.01)
b2 = nn.Parameter(torch.zeros(num_outputs, requires_grad=True))

params = [W1, b1, W2, b2]

def relu(x):
    a = torch.zeros_like(x)
    return torch.max(x, a)

def net(X):
    X = X.reshape((-1, num_inputs))
    H = relu(X@W1 + b1)
    return (H@W2 + b2)

loss = nn.CrossEntropyLoss(reduction='none')
num_epochs, lr = 10, 0.1
updater = torch.optim.SGD(params, lr=lr)
save_path=r'D:\Work\ML\d2l\code\outputs\prediction.png'
d2l.train_ch3(net, train_iter, test_iter, loss, num_epochs, updater)
d2l.predict_ch3(net, test_iter, save_path=save_path)


# ============================================================
# 课后练习：超参数实验分析与回答
# ============================================================

"""
【问题1】在所有其他参数保持不变的情况下，更改超参数num_hiddens的值，
并查看此超参数的变化对结果有何影响。确定此超参数的最佳值。

答：
num_hiddens（隐藏单元数）决定了模型的容量（表达能力）。

实验设置：固定 num_layers=1, lr=0.1, num_epochs=10，测试 num_hiddens ∈ {32, 64, 128, 256, 512, 1024}

预期结果趋势：
- num_hiddens=32:  模型容量过小，欠拟合，训练准确率和测试准确率都较低（约70%-75%）
- num_hiddens=64:  容量仍然偏小，但有所改善（约78%-82%）
- num_hiddens=128: 接近合适容量（约83%-86%）
- num_hiddens=256: 较好的平衡点（约85%-88%）
- num_hiddens=512: 容量充足，可能开始轻微过拟合（约86%-88%）
- num_hiddens=1024: 容量过大，容易过拟合，训练准确率高但测试准确率可能下降或持平

结论：
对于Fashion-MNIST数据集和单隐藏层架构，num_hiddens的最佳值在256-512之间。
256是一个性价比较高的选择（准确率与计算成本的平衡）。

关键洞察：
- 隐藏单元数太少 → 欠拟合（模型无法捕捉数据中的复杂模式）
- 隐藏单元数太多 → 过拟合风险增加（模型记住了训练数据的噪声）
- 最佳值取决于数据集的复杂度和数据量
"""

"""
【问题2】尝试添加更多的隐藏层，并查看它对结果有何影响。

答：
隐藏层数决定了网络的深度，影响模型的层次化特征学习能力。

实验设置：固定 num_hiddens=256, lr=0.1, num_epochs=10，测试 num_layers ∈ {1, 2, 3}

预期结果趋势：
- num_layers=1（基准）: 测试准确率约85%-88%
- num_layers=2: 测试准确率可能略有提升或持平（约86%-89%）
- num_layers=3: 测试准确率可能提升不明显，甚至下降（约85%-88%）

深层网络的影响分析：
1. 正向影响：
   - 更强的非线性表达能力
   - 层次化特征学习（底层学习边缘，高层学习形状）
   - 对于复杂数据集（如ImageNet），深层网络显著优于浅层

2. 负向影响（在Fashion-MNIST上）：
   - 数据集相对简单（28x28灰度图，10类），不需要太深的网络
   - 更深的网络更容易过拟合（参数量增加）
   - 训练难度增加（梯度消失/爆炸风险）
   - 计算成本增加

结论：
对于Fashion-MNIST这类简单数据集，1-2层隐藏层已足够。
添加更多隐藏层带来的收益有限，反而可能引入训练不稳定和过拟合问题。
深层网络的优势在更复杂的数据集（如CIFAR-10、ImageNet）上才能充分体现。
"""

"""
【问题3】改变学习速率会如何影响结果？保持模型架构和其他超参数
（包括轮数）不变，学习率设置为多少会带来最好的结果？

答：
学习率控制参数更新的步长，是神经网络训练中最重要的超参数之一。

实验设置：固定 num_hiddens=256, num_layers=1, num_epochs=10，测试 lr ∈ {0.001, 0.01, 0.1, 0.5, 1.0}

预期结果趋势：
- lr=0.001: 学习率太小，收敛极慢，10个epoch后可能远未收敛（准确率约60%-70%）
- lr=0.01: 学习率偏小，收敛较慢，但稳定（准确率约80%-85%）
- lr=0.1: 学习率适中，收敛快且稳定（准确率约85%-88%，最佳）
- lr=0.5: 学习率偏大，收敛快但可能震荡，可能跳过最优解（准确率约75%-85%）
- lr=1.0: 学习率过大，训练不稳定，损失可能发散（准确率约50%-70%）

学习率对训练过程的影响：
1. 学习率太小：
   - 收敛速度慢，需要更多epoch
   - 可能陷入局部最优
   - 训练时间成本增加

2. 学习率适中（如0.1）：
   - 收敛速度快
   - 损失函数平稳下降
   - 最终收敛到较好的局部最优

3. 学习率太大：
   - 损失函数震荡，甚至发散
   - 参数更新幅度过大，跳过最优解
   - 训练不稳定，准确率波动大

结论：
对于本实验（SGD优化器，单隐藏层MLP，Fashion-MNIST），
学习率0.1是较好的选择。实际最佳值可能在0.05-0.2之间，
需要通过更精细的搜索（如0.05, 0.08, 0.1, 0.15, 0.2）进一步确定。

注意：最佳学习率与优化器类型密切相关。
使用Adam优化器时，通常使用更小的学习率（如0.001-0.01）。
"""

"""
【问题4】通过对所有超参数（学习率、轮数、隐藏层数、每层的隐藏单元数）
进行联合优化，可以得到的最佳结果是什么？

答：
联合优化需要同时考虑所有超参数的交互作用。

实验方法：随机搜索（Random Search）
- 搜索空间：
  num_hiddens ∈ {64, 128, 256, 512}
  num_layers ∈ {1, 2, 3}
  lr ∈ {0.01, 0.05, 0.1, 0.3, 0.5}
  num_epochs ∈ {10, 20}

- 采样策略：随机采样15-20组组合进行实验

预期最佳组合（基于经验和理论分析）：
- num_hiddens: 256-512（提供足够的表达能力）
- num_layers: 1-2（Fashion-MNIST不需要太深）
- lr: 0.1左右（SGD的合适范围）
- num_epochs: 20（更多epoch允许更好收敛）

预期最佳结果：
测试准确率可能达到 88%-92%

相比单参数调优的提升来源：
1. 更多的epoch允许模型充分收敛
2. 合适的隐藏单元数提供足够的容量
3. 适中的深度避免过拟合
4. 匹配的学习率保证稳定快速收敛

重要提醒：
联合优化的最佳结果通常优于单参数调优，但搜索成本呈指数增长。
对于4个超参数，即使每个只有4-5个候选值，组合数也达到数百种。
因此需要高效的搜索策略（如随机搜索、贝叶斯优化）。
"""

"""
【问题5】描述为什么涉及多个超参数更具挑战性。

答：
多超参数优化比单参数优化困难得多，主要原因如下：

1. 维度灾难（Curse of Dimensionality）
   - 假设每个超参数有k个候选值，n个超参数的组合数为 k^n
   - 4个超参数，每个5个候选值 → 5^4 = 625 种组合
   - 实验成本随超参数数量指数增长

2. 超参数间的交互作用（Interaction Effects）
   - 超参数不是独立的，它们之间存在复杂的相互影响
   - 例如：学习率和batch size密切相关
     * 大batch size需要更大的学习率
     * 小batch size适合较小的学习率
   - 深度和宽度的权衡：深层窄网络 vs 浅层宽网络
   - 这种交互使得无法单独优化每个参数

3. 非凸优化景观（Non-convex Landscape）
   - 超参数空间通常是非凸的
   - 存在多个局部最优，梯度信息不可用
   - 无法使用标准的梯度下降方法

4. 评估成本高昂
   - 每组超参数都需要完整训练一个模型
   - 深度学习的训练时间从分钟到天数不等
   - 无法快速遍历所有组合

5. 噪声和不稳定性
   - 训练结果有随机性（权重初始化、数据打乱）
   - 相同的超参数可能得到不同的结果
   - 需要多次运行取平均，进一步增加成本

6. 不同尺度和类型
   - 学习率是连续值（0.0001到1.0）
   - 层数是离散整数
   - 优化器类型是类别变量
   - 需要不同的搜索策略

类比理解：
单参数调优就像在一条山路上寻找最低点。
多参数调优就像在高维山脉中寻找最低点，
每增加一个维度，搜索空间就呈指数增长。
"""

"""
【问题6】如果想要构建多个超参数的搜索方法，请想出一个聪明的策略。

答：
以下是几种高效的超参数搜索策略，从简单到复杂：

【策略1】随机搜索（Random Search）⭐ 推荐入门
原理：在搜索空间中随机采样，而非网格遍历
优点：
- 实现简单
- 比网格搜索更高效（研究表明随机搜索通常优于网格搜索）
- 可以灵活控制预算（想试多少组就试多少组）
实现：
```python
import random
for _ in range(n_trials):
    lr = 10 ** random.uniform(-3, 0)  # 对数尺度采样
    num_hiddens = random.choice([64, 128, 256, 512])
    # ... 训练并评估
```

【策略2】贝叶斯优化（Bayesian Optimization）
原理：使用高斯过程或树结构Parzen估计器（TPE）构建超参数与性能的代理模型，
      选择最有希望的超参数组合进行下一次评估
优点：
- 样本效率高（用更少的试验找到更好的解）
- 自动平衡探索与利用
工具：Optuna, Hyperopt, scikit-optimize

【策略3】早停策略（Early Stopping / Successive Halving）
原理：先对所有候选配置进行少量epoch的训练，淘汰表现差的，
      对表现好的配置增加训练资源
优点：
- 大幅节省计算资源
- 避免在明显差的配置上浪费太多时间
实现（Hyperband算法）：
```
1. 随机采样N组超参数
2. 每组训练1个epoch，保留前50%
3. 幸存者训练3个epoch，保留前50%
4. 幸存者训练9个epoch，保留前50%
5. 最后幸存者训练27个epoch
```

【策略4】基于重要性的搜索（Hyperparameter Importance）
原理：先评估哪些超参数对结果影响最大，优先优化重要参数
方法：
- fANOVA分析：量化每个超参数的贡献
- 偏依赖图：可视化单个超参数的影响

【策略5】迁移学习/元学习（Meta-Learning）
原理：利用历史任务（类似数据集）的最优超参数作为先验
方法：
- 学习数据集特征与最佳超参数之间的映射
- 对于新任务，快速推荐合适的超参数范围

【策略6】自动化机器学习（AutoML）
工具：AutoGluon, Auto-sklearn, H2O AutoML
特点：
- 完全自动化超参数搜索
- 集成多种模型和策略
- 适合非专家用户

【推荐实践方案】
对于本实验（Fashion-MNIST MLP），推荐以下流程：

Step 1: 粗粒度随机搜索（20-30组）
  - 快速确定大致范围
  - 使用较少的epoch（如5-10）

Step 2: 细粒度网格搜索
  - 在Step 1确定的最佳区域进行精细搜索
  - 使用完整的epoch数

Step 3: 验证最佳配置
  - 多次运行取平均，消除随机性
  - 评估训练集和测试集表现

关键技巧：
1. 对数尺度采样：学习率、正则化系数等使用对数尺度
2. 分层验证：使用交叉验证评估稳定性
3. 记录所有实验：使用工具（如MLflow, Weights & Biases）跟踪
4. 早停：设置验证集损失不下降的 patience

总结：
没有"最好"的搜索策略，只有"最适合当前资源约束"的策略。
计算资源有限 → 随机搜索 + 早停
计算资源充足 → 贝叶斯优化 + 完整验证
追求极致效果 → AutoML工具
"""
