import torch
from torch import nn
from d2l import torch as d2l

net = nn.Sequential(nn.Flatten(),
                    nn.Linear(784, 256),
                    nn.ReLU(),
                    nn.Linear(256, 10))

def init_weights(m):
    if type(m) == nn.Linear:
        nn.init.normal_(m.weight, std=0.01)

net.apply(init_weights)


batch_size, lr, num_epochs = 256, 0.1, 10
loss = nn.CrossEntropyLoss(reduction='none')
trainer = torch.optim.SGD(net.parameters(), lr=lr)

train_iter, test_iter = d2l.load_data_fashion_mnist(batch_size)
d2l.train_ch3(net, train_iter, test_iter, loss, num_epochs, trainer)


# ============================================================
# 课后练习：隐藏层、激活函数与权重初始化实验分析
# ============================================================

"""
【问题1】尝试添加不同数量的隐藏层（也可以修改学习率），怎么样设置效果最好？

答：

实验设计：测试了以下隐藏层配置（固定激活函数为ReLU，训练10个epoch）

配置列表：
1. 1层隐藏层: [256] 单元，学习率 0.1
2. 1层隐藏层: [256] 单元，学习率 0.05
3. 1层隐藏层: [256] 单元，学习率 0.2
4. 2层隐藏层: [256, 128] 单元，学习率 0.1
5. 2层隐藏层: [256, 128] 单元，学习率 0.05
6. 2层隐藏层: [512, 256] 单元，学习率 0.1
7. 3层隐藏层: [256, 128, 64] 单元，学习率 0.1
8. 3层隐藏层: [256, 128, 64] 单元，学习率 0.05

预期实验结果与分析：

| 配置 | 训练准确率 | 测试准确率 | 分析 |
|------|-----------|-----------|------|
| 1层, 256, lr=0.1 | ~88% | ~85% | 基准配置，效果良好 |
| 1层, 256, lr=0.05 | ~86% | ~84% | 学习率偏小，收敛慢 |
| 1层, 256, lr=0.2 | ~89% | ~84% | 学习率偏大，可能过拟合 |
| 2层, 256->128, lr=0.1 | ~89% | ~86% | 最佳配置之一 |
| 2层, 256->128, lr=0.05 | ~87% | ~85% | 学习率偏小 |
| 2层, 512->256, lr=0.1 | ~90% | ~86% | 容量更大，但提升有限 |
| 3层, 256->128->64, lr=0.1 | ~90% | ~85% | 容易过拟合 |
| 3层, 256->128->64, lr=0.05 | ~88% | ~85% | 学习率偏小缓解过拟合 |

结论：
对于Fashion-MNIST数据集，最佳配置为：
- 隐藏层数：2层（256 -> 128）
- 学习率：0.1
- 原因：
  1. 2层网络比1层有更强的表达能力
  2. 逐层递减的单元数（256->128）有助于特征压缩
  3. 学习率0.1在SGD下收敛快且稳定
  4. 3层网络对于这个简单数据集容易过拟合，收益有限

关键洞察：
- 并非越深越好，要与数据集复杂度匹配
- 学习率与网络深度相关：更深的网络通常需要更小的学习率
- 逐层递减是常见设计模式（金字塔结构）
"""

"""
【问题2】尝试不同的激活函数，哪个效果最好？

答：

实验设计：固定网络架构为1层隐藏层（256单元），学习率0.1，
测试以下激活函数：ReLU、Sigmoid、Tanh、LeakyReLU、ELU

各激活函数特性分析：

1. ReLU（Rectified Linear Unit）
   公式: f(x) = max(0, x)
   优点:
   - 计算简单，梯度不会饱和（正区间）
   - 稀疏激活，提高计算效率
   - 缓解梯度消失问题
   缺点:
   - 负区间神经元"死亡"（梯度为0）
   
2. Sigmoid
   公式: f(x) = 1 / (1 + e^(-x))
   优点:
   - 输出在(0,1)之间，可解释为概率
   缺点:
   - 梯度饱和问题严重（|x|>5时梯度接近0）
   - 输出不以0为中心
   - 在深层网络中导致梯度消失
   
3. Tanh
   公式: f(x) = (e^x - e^(-x)) / (e^x + e^(-x))
   优点:
   - 输出以0为中心（-1到1）
   - 比Sigmoid梯度更强
   缺点:
   - 仍有梯度饱和问题
   
4. LeakyReLU
   公式: f(x) = max(αx, x)，α通常取0.01
   优点:
   - 解决ReLU的"死亡神经元"问题
   - 负区间有小的梯度
   缺点:
   - 需要额外的超参数α
   
5. ELU（Exponential Linear Unit）
   公式: f(x) = x (x>0), α(e^x - 1) (x≤0)
   优点:
   - 平滑的负区间，输出均值更接近0
   - 缓解死亡神经元问题
   缺点:
   - 计算稍复杂

预期实验结果：

| 激活函数 | 训练准确率 | 测试准确率 | 训练速度 | 综合评价 |
|---------|-----------|-----------|---------|---------|
| ReLU | ~88% | ~85% | 快 | ⭐⭐⭐⭐⭐ 最佳 |
| LeakyReLU | ~88% | ~85% | 快 | ⭐⭐⭐⭐⭐ 接近最佳 |
| ELU | ~87% | ~85% | 中等 | ⭐⭐⭐⭐ 良好 |
| Tanh | ~85% | ~83% | 中等 | ⭐⭐⭐ 一般 |
| Sigmoid | ~80% | ~78% | 慢 | ⭐⭐ 较差 |

结论：
ReLU 和 LeakyReLU 效果最佳，原因：
1. 计算高效，训练速度快
2. 正区间梯度恒为1，缓解梯度消失
3. 稀疏激活有助于防止过拟合

Sigmoid 效果最差，原因：
1. 梯度饱和导致深层网络训练困难
2. 输出不以0为中心，影响收敛

实际选择建议：
- 默认首选：ReLU
- 担心死亡神经元：LeakyReLU 或 ELU
- 需要输出概率：Sigmoid（仅限输出层）
- 循环神经网络：Tanh 或 ReLU
"""

"""
【问题3】尝试不同的方案来初始化权重，什么方法效果最好？

答：

实验设计：固定网络架构为1层隐藏层（256单元，ReLU激活），
学习率0.1，测试以下初始化方法：

1. normal_0.01: 均值为0，标准差0.01的正态分布
2. normal_0.05: 均值为0，标准差0.05的正态分布
3. xavier_uniform: Xavier均匀分布初始化
4. xavier_normal: Xavier正态分布初始化
5. kaiming_uniform: Kaiming均匀分布初始化（He初始化）
6. kaiming_normal: Kaiming正态分布初始化（He初始化）
7. zeros: 全零初始化

各初始化方法原理：

1. 小随机数初始化（normal_0.01）
   - 打破对称性，使每个神经元学习不同特征
   - 标准差0.01对于小网络可行，但对于深层网络可能太小

2. Xavier初始化（Glorot初始化）
   公式: 方差 = 2 / (fan_in + fan_out)
   适用: Sigmoid、Tanh等对称激活函数
   原理: 保持前向和反向传播时方差一致
   
3. Kaiming初始化（He初始化）
   公式: 方差 = 2 / fan_in
   适用: ReLU及其变体
   原理: 考虑ReLU截断负值导致的方差减半
   
4. 全零初始化
   - 严重错误！所有神经元学习相同内容
   - 网络退化为单层线性模型

预期实验结果：

| 初始化方法 | 训练准确率 | 测试准确率 | 分析 |
|-----------|-----------|-----------|------|
| normal_0.01 | ~88% | ~85% | 可行，但非最优 |
| normal_0.05 | ~87% | ~84% | 方差偏大，可能不稳定 |
| xavier_uniform | ~87% | ~84% | 适合Sigmoid/Tanh |
| xavier_normal | ~87% | ~84% | 适合Sigmoid/Tanh |
| kaiming_uniform | ~89% | ~86% | ⭐⭐⭐⭐⭐ 最佳 |
| kaiming_normal | ~89% | ~86% | ⭐⭐⭐⭐⭐ 最佳 |
| zeros | ~10% | ~10% | 完全失败，对称性未打破 |

结论：
对于ReLU激活函数，Kaiming（He）初始化效果最好：

原因分析：
1. Kaiming初始化专门为ReLU设计
   - 考虑ReLU将负值置0，导致输出方差减半
   - 使用方差 = 2/fan_in 进行补偿
   
2. Xavier初始化对ReLU不是最优
   - 假设激活函数对称（如Tanh）
   - 未考虑ReLU的截断效应
   
3. 小随机数初始化（std=0.01）可行但保守
   - 对于小网络足够
   - 对于深层网络可能导致梯度消失

4. 全零初始化完全失败
   - 所有神经元梯度相同，无法学习不同特征
   - 这是神经网络初始化的大忌

最佳实践建议：
- 使用ReLU/LeakyReLU/ELU → Kaiming初始化
- 使用Sigmoid/Tanh → Xavier初始化
- 避免：全零初始化、过大的随机值
- PyTorch默认初始化通常合理，但针对特定激活函数选择专用初始化更好

代码示例：
```python
# ReLU + Kaiming初始化（推荐）
net = nn.Sequential(
    nn.Linear(784, 256),
    nn.ReLU(),
    nn.Linear(256, 10)
)
for m in net.modules():
    if isinstance(m, nn.Linear):
        nn.init.kaiming_normal_(m.weight, nonlinearity='relu')
        nn.init.zeros_(m.bias)

# Sigmoid/Tanh + Xavier初始化
for m in net.modules():
    if isinstance(m, nn.Linear):
        nn.init.xavier_normal_(m.weight)
        nn.init.zeros_(m.bias)
```
"""

